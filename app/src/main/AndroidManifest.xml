<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cc.captaincode.hurrileaf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.goodsDonation.ui.GoodsDonationActivity"/>
        <activity android:name=".activities.paymentDonation.ui.PaymentDonationActivity" />
        <activity android:name=".activities.sponsors.ui.SponsorFormActivity" />
        <activity android:name=".activities.donations.ui.DonationTypeActivity" />
        <activity android:name=".activities.home.ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>